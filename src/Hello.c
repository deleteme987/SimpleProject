#include <stdio.h>


// Addded some comments
int main() {

	// Simple  example of haow to add something in C

   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0;
}